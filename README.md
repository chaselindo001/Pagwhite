# Clone: Santa Helena Serviços Financeiros LTDA

> Capturado de https://cliente-servicos-online.netlify.app/ em 2026-08-09T21:04:29.531Z por Nullain Cloner.

## Estrutura

- `index.html` — DOM limpo e semântico, apontando para assets locais
- `styles.css` — design tokens em `:root` + @font-face + CSS original completo + utilities
- `design-tokens.json` — 28 tokens (cores, espaçamentos, tipografia…)
- `animations.spec.json` — 1 specs de animação (CSS + JS detectadas)
- `components.json` — 8 componentes detectados (slots, variantes, tokens)
- `clone-meta.json` — metadados completos da captura
- `assets/` — 0 arquivos locais (images/, fonts/, svgs/, styles/)

## Stack detectada

- Frameworks: —
- Styling: —
- Animações: —
- CMS/Builder: —

## Como usar com IA (Cursor / Claude Code)

Abra esta pasta no editor e peça, por exemplo:
"Reimplemente este layout como componentes React usando os tokens de design-tokens.json
e as animações de animations.spec.json."


